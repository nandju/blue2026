export const metadata = {
  title: "Administration — BLUE",
  description: "Tableau de bord administrateur BLUE",
  robots: "noindex, nofollow",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
